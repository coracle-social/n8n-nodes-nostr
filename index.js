// n8n loads nodes and credentials from the `n8n` key in package.json, which
// points at the compiled files under dist/. This entry point exists only so the
// package has a valid `main`; it intentionally exports nothing.
module.exports = {}
