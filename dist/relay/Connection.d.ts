import type { Event, Filter } from '../nostr';
import { AuthState, ConnState } from './types';
import type { AuthOptions, PerRelayResult, ReqHandle, ReqHandlers } from './types';
export declare class Connection {
    readonly url: string;
    state: ConnState;
    authState: AuthState;
    private auth;
    private ws?;
    private sendQueue;
    private subs;
    private publishes;
    private challenge?;
    private challengeWaiters;
    private authPromise?;
    private authEventId?;
    private authSettle?;
    private authFailReason;
    autoReconnect: boolean;
    private disposed;
    private backoffAttempt;
    private cancelReconnect?;
    private connectPromise?;
    private closeWaiters;
    constructor(url: string, auth: AuthOptions);
    private canAuth;
    setAuth(auth: AuthOptions): void;
    connect(timeoutMs?: number): Promise<void>;
    private handleClose;
    private resubscribeAll;
    private send;
    private flushSendQueue;
    private releaseChallengeWaiters;
    private waitForChallenge;
    private ensureAuth;
    private performAuth;
    private handleMessage;
    publish(event: Event, timeoutMs: number): Promise<PerRelayResult>;
    req(subId: string, filters: Filter[], handlers: ReqHandlers): ReqHandle;
    updateFilters(subId: string, filters: Filter[]): void;
    close(): Promise<void>;
}
